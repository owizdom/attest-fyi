{
  "name": "attest-fail-web",
  "private": true,
  "description": "attest.fail frontend (TypeScript, compiled to app.js)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
